<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Thin activity shell theme. Compose draws the actual UI (Material 3 via
      OpenAndroidUseTheme), so the platform theme only needs to be a no-action-bar
      container. Using a platform parent keeps the app off AppCompat/androidx
      beyond the Compose libraries already added for the presentation layer.
    -->
    <style name="Theme.OpenAndroidUse" parent="@android:style/Theme.Material.Light.NoActionBar" />

    <!--
      Phase 4.7a: Android 12+ splash. Brand indigo ground + the monochrome brand
      mark; hands off to the real (Compose) theme once the launcher Activity calls
      installSplashScreen(). Applied to MainActivity only.
    -->
    <style name="Theme.OpenAndroidUse.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_monochrome</item>
        <item name="postSplashScreenTheme">@style/Theme.OpenAndroidUse</item>
    </style>
</resources>
