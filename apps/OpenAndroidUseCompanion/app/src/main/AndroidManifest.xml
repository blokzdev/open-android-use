<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Push-to-talk task input (Phase 3.1c). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Ongoing "agent is acting" notification with a Stop action (Phase 4.4);
         Android 13+ runtime permission, requested from the chat screen. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- The agent's list_apps / app resolution must see every launchable app;
         Android 11+ package-visibility filtering would otherwise hide most of
         them. Sideloaded/debug distribution, so the Play policy on this
         permission does not apply. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:enableOnBackInvokedCallback="true"
        android:resizeableActivity="true"
        android:theme="@style/Theme.OpenAndroidUse">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.OpenAndroidUse.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- singleTask: one Chat instance. Resuming a session from History routes
             back into it (via onNewIntent) instead of stacking duplicates. -->
        <activity
            android:name=".agent.ChatActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:label="Agent" />

        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:label="About" />

        <activity
            android:name=".agent.SettingsActivity"
            android:exported="false"
            android:label="Settings" />

        <activity
            android:name=".PrivacyActivity"
            android:exported="false"
            android:label="Privacy &amp; data" />

        <activity
            android:name=".agent.SessionsActivity"
            android:exported="false"
            android:label="History" />

        <activity
            android:name=".OnboardingActivity"
            android:exported="false"
            android:label="Set up" />

        <!-- Shares the exported-conversation Markdown file (Phase 4.5) without
             world-readable storage; grants are per-share and read-only. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>


        <receiver
            android:name=".StopAgentReceiver"
            android:exported="false" />

        <service
            android:name=".CompanionService"
            android:exported="false"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Phase 5.5b: optional native libraries the LiteRT-LM GPU backend uses
             when present (CPU still works without them). required=false so the
             app installs on devices that lack them. -->
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <uses-native-library android:name="libvndksupport.so" android:required="false" />
    </application>
</manifest>
