<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Push-to-talk task input (Phase 3.1c). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Ongoing "agent is acting" notification with a Stop action (Phase 4.4);
         Android 13+ runtime permission, requested from the chat screen. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- The agent's list_apps / app resolution must see every launchable app;
         Android 11+ package-visibility filtering would otherwise hide most of
         them. Sideloaded/debug distribution, so the Play policy on this
         permission does not apply. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.OpenAndroidUse">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".agent.ChatActivity"
            android:exported="false"
            android:label="Agent" />

        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:label="About" />

        <activity
            android:name=".OnboardingActivity"
            android:exported="false"
            android:label="Set up" />


        <receiver
            android:name=".StopAgentReceiver"
            android:exported="false" />

        <service
            android:name=".CompanionService"
            android:exported="false"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
