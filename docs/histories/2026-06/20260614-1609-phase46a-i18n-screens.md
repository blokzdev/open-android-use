## [2026-06-14 16:09] | Task: Phase 4.6a-1 — externalize static-screen strings (i18n)

### 🤖 Execution Context
* **Agent ID**: `claude-code (session 013vN6M9RAYBQnDAybhdYE7T)`
* **Base Model**: `claude-opus-4-8[1m]`
* **Runtime**: `Claude Code on the web (remote)`

### 📥 User Query
> Proceed with Phase 4.6 (a11y/i18n/responsive). Scope: i18n externalize-only,
> tablet/foldable two-pane, markdown links+tables. This is the first i18n PR.

### 🛠 Changes Overview
**Scope:** `apps/OpenAndroidUseCompanion` presentation layer; docs/roadmap.

**Key Actions:**
- **Consolidated `res/values/strings.xml`** (~11 → ~120 entries) with shared keys and
  positional format args (`main_service_running` `%1$d`, `onboarding_step` `%1$d/%2$d`,
  `sessions_more_options`/`sessions_delete_body` `%1$s`, `about_version`).
- **Externalized the static screens** to `stringResource(...)`: `MainActivity`,
  `OnboardingActivity`, `PrivacyActivity`, `AboutActivity`, `agent/SettingsActivity`,
  `agent/SessionsActivity` (incl. Activity toasts via `getString`).
- **Externalized the non-Compose surfaces** via `context.getString`: `agent/InControlOverlay`,
  `agent/AgentNotification`, `agent/ConfirmationSheet`.
- **Docs**: new Phase 4.6 sub-plan; roadmap 4.6 → in-progress + a new future **Localization**
  phase + decision; this history.

**Deferred to 4.6a-2:** `agent/ChatActivity` (~40 strings) and the `agent/AgentController`
transcript notes (need a `Context` threaded into the agent singleton) + `ToolChipLabel`;
the lint `HardcodedText` guard is enabled after those land (no baseline needed).

### 🧠 Design Intent (Why)
i18n is externalize-only: the UI becomes fully translation-ready while staying English, so the
framework lands now without a translation-quality/maintenance commitment (actual languages →
future Localization phase). 4.6a is split into two PRs so the large, recently-churned
`ChatActivity` and the security-sensitive `AgentController` notes get their own reviewable diff
and the tree stays green throughout.

### 📁 Files Modified
- `apps/OpenAndroidUseCompanion/app/src/main/res/values/strings.xml`
- `.../companion/{MainActivity,OnboardingActivity,PrivacyActivity,AboutActivity}.kt`
- `.../companion/agent/{SettingsActivity,SessionsActivity,InControlOverlay,AgentNotification,ConfirmationSheet}.kt`
- `docs/exec-plans/active/20260614-phase46-a11y-i18n-responsive.md` (new)
- `docs/exec-plans/active/20260612-android-use-runtime.md`
