## [2026-06-14 14:59] | Task: Phase 4.5.1 hardening + Compose UI test enablers

### 🤖 Execution Context
* **Agent ID**: `claude-code (session 013vN6M9RAYBQnDAybhdYE7T)`
* **Base Model**: `claude-opus-4-8[1m]`
* **Runtime**: `Claude Code on the web (remote)`

### 📥 User Query
> Before Phase 4.6 — what should we tackle? Selected: fix the 4.5 regressions, stand up
> Compose UI test infra, expand emulator-smoke, move Play work to a dedicated Phase 4.7,
> and capture any other quick wins.

### 🛠 Changes Overview
**Scope:** `apps/OpenAndroidUseCompanion` (presentation Activities + `agent` package + tests);
docs/roadmap.

**Key Actions:**
- **Material You re-theme** of back-stack screens: `ChatActivity`/`MainActivity` recreate on
  resume when `settings.dynamicColor` changed since onCreate (toggle now applies app-wide).
- **`ChatActivity` singleTask + `onNewIntent`**: resuming a session from History routes into
  the single Chat instance instead of stacking duplicates.
- **Accessibility**: `contentDescription` for the mic button, the History `⋯` overflow, and
  the in-control overlay chip.
- **Session-save churn guard**: `AgentController.transcriptRevision` (bumped in `log()`/
  `restore()`); `ChatActivity` persists only on change, so opening Settings/History no longer
  reshuffles History order.
- **Naming**: `ChatScreen` `onShare` → `onExport`.
- **Compose UI test infrastructure**: added `ui-test-junit4`/`ui-test-manifest`; new
  instrumented tests `SettingsScreenTest`, `SessionsScreenTest`, `AgentSettingsInstrumentedTest`
  (real Keystore round-trip) — all run in the existing `emulator-smoke` pass, expanding
  automated VERIFICATION coverage (V33 keystore, V62/V70 UI).
- **Docs**: split Play-readiness into a new **Phase 4.7** in the roadmap (moved the
  Distribution bullet there; added `QUERY_ALL_PACKAGES` policy, FGS type, listing/privacy
  policy); 4.5.1 progress + decision; sub-plan; VERIFICATION CI note; QUALITY_SCORE.

### 🧠 Design Intent (Why)
Fix the regressions 4.5 introduced and lay a UI-test safety net *before* the large Phase 4.6
string-externalization touches every screen, so that refactor is verifiable. `singleTask`
gives a clean single-workspace model for History resume. Splitting Play into 4.7 keeps 4.6 a
focused UX/i18n phase. Compose UI tests are instrumented and ride the existing emulator CI,
which doubles as automated hardware-ledger coverage rather than adding a Robolectric stack.

### 📁 Files Modified
- `apps/OpenAndroidUseCompanion/app/src/main/kotlin/dev/openandroiduse/companion/agent/ChatActivity.kt`
- `apps/OpenAndroidUseCompanion/app/src/main/kotlin/dev/openandroiduse/companion/MainActivity.kt`
- `apps/OpenAndroidUseCompanion/app/src/main/kotlin/dev/openandroiduse/companion/agent/AgentController.kt`
- `apps/OpenAndroidUseCompanion/app/src/main/kotlin/dev/openandroiduse/companion/agent/SessionsActivity.kt`
- `apps/OpenAndroidUseCompanion/app/src/main/kotlin/dev/openandroiduse/companion/agent/InControlOverlay.kt`
- `apps/OpenAndroidUseCompanion/app/src/main/AndroidManifest.xml`, `app/build.gradle.kts`
- `apps/OpenAndroidUseCompanion/app/src/androidTest/.../agent/{SettingsScreenTest,SessionsScreenTest,AgentSettingsInstrumentedTest}.kt` (new)
- `docs/exec-plans/active/20260614-phase451-hardening.md` (new), `docs/exec-plans/active/20260612-android-use-runtime.md`
- `VERIFICATION.md`, `docs/QUALITY_SCORE.md`
