## [2026-06-12 06:10] | Task: Phase 3.1a execution plan — decision-complete blueprint for the on-device agent

### 🤖 Execution Context
* **Agent ID**: Claude Code (cloud session, post-merge of PR #1)
* **Base Model**: claude-fable-5
* **Runtime**: Claude Code remote execution container

### 📥 User Query
> Continue looping toward the on-device Second-Pair-of-Hands agent experience.

### 🛠 Changes Overview
**Scope:** docs only (exec plan).

**Key Actions:**
- **[Exec plan]**: `docs/exec-plans/active/20260612-phase3-on-device-agent.md`
  — binding decisions for the 3.1a build, sourced from the claude-api skill:
  official Anthropic Java SDK from Kotlin (zero-dep rule re-scoped to the
  control surface), default model claude-opus-4-8 with adaptive thinking and
  effort=high, streaming all turns, explicit refusal stop-reason handling,
  frozen tools+system prompt-caching prefix, manual agentic loop (needed for
  the pause/consent gate), 9-tool schema executed in-process, Keystore-held
  API key. Milestone 1 is an SDK-on-Android spike that gates the rest.

### 🧠 Design Intent (Why)
The session's context budget was better spent banking correct API decisions
than rushing a large Kotlin build; the plan makes the next session productive
from minute one (memory harness working as designed). Notably it records an
amendment to a prior decision (companion zero-dependency rule) explicitly
rather than silently violating it.

### 📁 Files Modified
- `docs/exec-plans/active/20260612-phase3-on-device-agent.md`
