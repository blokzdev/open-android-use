## [2026-06-16 07:59] | Task: 写 Agent 安全/信任架构 SPEC 并对齐文档（6.5c-0）

### 🤖 Execution Context
* **Agent ID**: `claude-code (session 01SteoxNdFSThg2ULfifResK)`
* **Base Model**: `Opus 4.8 (1M)`
* **Runtime**: `Claude Code on the web (remote)`

### 📥 User Query
> Kickstart 6.5c. Research deeply (verify the prior trust-model research, arxiv where
> applicable) and implement maximally as per the best-in-class "second pair of hands"
> vision, across as many 6.5c PRs as needed. Founder calls this round: secrets always
> hand off (never agent-typed); add an injection-hardening track; build login tap-to-fill;
> and **lead with a permanent Security & Trust Architecture spec** (Google's layered defense
> + CaMeL principles) as a reference for this/other projects, aligning the repo docs.

### 🛠 Changes Overview
**Scope:** docs only (`docs/design-docs`, `docs/SECURITY.md`, `docs/BACKLOG.md`,
`docs/QUALITY_SCORE.md`, `docs/exec-plans/active`). No code.

**Key Actions:**
- **New spec**: `docs/design-docs/agent-security-trust-architecture.md` — the durable reference.
  Threat model (environmental injection, lethal trifecta, sensitive-screen leak, a11y-API
  tightening); the cardinal rule (secrets never enter the model loop); the **layered defense
  model** with honest build/rely/defer per layer; the **3-layer structural enforcement invariant**
  ("a granted app still hands off secrets" by construction); CaMeL **principles adopted / full
  system deferred**; OS-mediated credential entry + Play-policy fragility; provider model-hardening
  reliance + on-device tier limitation; external-MCP-runtime boundary; primary-source references.
- **Doc alignment**: expanded the 6.5c line in the Phase 6 exec-plan into 6.5c-0..-5; SECURITY.md
  gained an English "Sensitive-screen trust model & injection posture" section; BACKLOG promoted
  6.5c out and added full-CaMeL + region-mask-screenshot follow-ups; QUALITY_SCORE security row +
  design-docs index updated.

### 🧠 Design Intent (Why)
Research turned "per-app always allow" from a single dangerous toggle into a layered, least-privilege
trust model. The founder wanted the security thinking captured as a **permanent, reusable reference**
before code — so the architecture (and its honest limits: what we build vs. rely on vs. defer) is
versioned in the repo, drives the 6.5c-1..-5 PRs, and survives context. Doc-first also lets the
"secrets never enter the model loop" red line be stated once and enforced structurally everywhere.

### 📁 Files Modified
- `docs/design-docs/agent-security-trust-architecture.md` (new)
- `docs/design-docs/index.md`
- `docs/SECURITY.md`
- `docs/BACKLOG.md`
- `docs/QUALITY_SCORE.md`
- `docs/exec-plans/active/20260616-phase6-world-class.md`
